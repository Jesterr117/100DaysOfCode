class Solution {
    public int findExtra(int a[], int b[], int n) {
        // add code here.
        int i = 0;

       
        while (i < n && i < b.length && a[i] == b[i]) {
            i++;
        }

        
        return i;
    }
}
