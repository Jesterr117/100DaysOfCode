class Solution {
    static int countSquares(int N) {
        // code here
        if (N <= 0) {
            return 0; 
        }

        int count = 0;
        for (int i = 1; i * i < N; i++) {
            count++;
        }
        return count;
    }
}
